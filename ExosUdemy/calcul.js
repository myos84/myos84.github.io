let total = 0;

function addition () { 

    for(let i = 1 ; i <= chiffreSaisi; i++){
        total += i
        console.log(`Step : ${i} total : ${total} `);
    }
    
     }


     module.exports = calcul;